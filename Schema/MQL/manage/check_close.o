[Main]
Name=CheckClose
Title=Проверка закрытия
Group=Управление

[Input]
Ticket=n

[Code]
<lines>
  if (OrderSelect($Ticket, SELECT_BY_TICKET, MODE_TRADES) && OrderCloseTime()!=0)
  {
$CTRL.lines
  }

[Help]
Передаёт управление при закрытии ордера.
