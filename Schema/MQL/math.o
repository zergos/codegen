[Main]
Unit
Name=Math
Title=Модуль математ.
Group=Программы

[Code]
<procs>
int MathSign(int a)
{
  if (a>0) return (1);
  else if (a<0) return (-1);
  else return (0);
}

double MathSqr(double x)
{
  return x*x;
}

int toint(double x) { return (x+0.5); }
